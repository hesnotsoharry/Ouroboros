# Agent IDE (Ouroboros) Wave Temperature Log

Per-wave one-line tag capturing pain level. See `C:\Web App\docs\wave-temperature-log-template.md` for definitions and rules.

## Rules

- **No backfilling from memory.** Start from the next wave shipped after 2026-05-12.
- **Update at wave-end (Stage 7 Handoff).**

## Entries

| Wave | Date shipped | Temp | One-line note |
|---|---|---|---|
| W-87 | (in flight) | TBD | Wave 87 (chat orchestration rework) was in flight at log creation (2026-05-12); first entry pending wave ship |
| M-4 (Pipeline Hardening Wave M-4 — Agent IDE Electron e2e to CI) | 2026-05-12 | TEETH-PULLING | E2e suite had deep drift: 11 of 22 specs failed against current code (theme color regression, IPC contract drift, real renderer page errors on cold launch, Windows teardown hang in fixture, vitest-import crash blocking the whole suite). Phase 1 stabilized harness to a 9-test "stable subset" by fixing the fixture's missing `page.close()` workaround + bumping timeout 30s→60s + testIgnore for 6 drift-broken specs + `test.fixme` for 1 bug-catching test. Phase 2 wired CI on Ubuntu under xvfb. 11 individual test failures filed as follow-up (real bugs, not test drift — need a future bug-fix wave). The "Playwright + Electron wrinkle" the meta-spec named is closed for the stable subset; full coverage gated on bug-fix wave |
| W-88 (Terminal Foundation) | 2026-05-14 | TEETH-PULLING | Wave body itself was clean — 6 phases via subagent dispatch, mechanical review PASS, full local suite 1065/1065. Pain concentrated in smoke + ship tail: manual smoke surfaced 4 real bugs (unicode addon version string, destructive dock-height migration, WebGL context-loss white flash, misplaced eslint-disable) — all fixed in-wave. Ship tail rough: an orchestrator merge-on-red slip, then discovery that master CI was *already* red pre-wave (handoff's "green at cut point" was wrong) across all 3 platforms with ≥3 distinct causes. Lockfile-divergence hypothesis investigated + refuted; macOS Electron-binary cause fixed; Ubuntu+Windows failures filed as a tracked bug. Shipped partial (macOS green, Ubuntu/Windows tracked). 5 follow-ups filed total |
| W-92 (Cross-Platform Lockfile + Stryker) | 2026-05-16 | TEETH-PULLING | 8 implementation phases shipped cleanly via subagent dispatch (3 boundary phases with orchestrator-authored acceptance tests, all PASS). Stryker baseline 22.41% (174 mutants, 31 src/shared files) at first try, break: 21 armed. Pain at PR/CI tail: Phase 5's `lockfile:sync` from-scratch regen produced a complete cross-platform lockfile but with drifted transitives (vite 7.3.1 → 7.3.3 + many Babel transforms), causing 1077 renderer test failures on ALL 3 OS CI (`ReferenceError: React is not defined`). Pinning vite alone via `overrides` wasn't sufficient — too many transitives also shifted. Resolved by reverting package-lock.json to master + `npm install --package-lock-only` adding ONLY Stryker tree (preserves master's known-good transitive resolution). Marker regenerated with honest `generatedBy: 'wave-92-phase-9-revert-and-add'`. Foundation tooling (lockfile:sync, check, hooks, CI canary, ci-stryker.yml) shipped fully — the regen wrapper works mechanically, just produces drift Agent IDE's React/vite stack doesn't tolerate. 2 follow-ups filed (mutate-scope expansion + transitive-drift gap with recommended drift-check script) |
| W-93 (Fix Sweep: Lockfile Drift Check + Cleanups) | 2026-05-16 | MILD | Four small follow-ups bundled, ~3 hours wall-clock. A/B/D dispatched in parallel (no shared files), C blocked on A. Phase A clean — sonnet-implementer shipped 176-line drift checker + 6-case test suite + wrapper integration first pass. Phase B clean — haiku-implementer did 5 mechanical log-level edits; one missed site (`ContextPreview.popover.tsx`) caught by orchestrator grep and self-fixed. Phase D clean — haiku-implementer stubbed the dead component for orchestrator rm, missed one CLAUDE.md reference (caught + fixed). Phase C bumpy — sonnet-implementer stalled at ~9min on web-tree-sitter 0.25+'s ESM rewrite (default→named export, `Parser.SyntaxNode → Node`, wasm export-key resolution); orchestrator recovered via (1) test-import update to new API, (2) haiku-implementer dispatch for 54-occurrence mechanical rename across 4 files, (3) self-fix of 11 missed references in the main parser file. Boundary acceptance test held — preserved the contract while the implementation churned. Lockfile via `npm install --package-lock-only` for minimal-delta bump (avoided full WSL2 regen drift). No Tier 3. New `.claude/vendor-gotchas/tree-sitter.md` captures the 0.22→0.26 migration lessons |
| W-94 + W-96 (Chat-Workbench Completion + emergency type-coupling unblock) | 2026-05-18 | HOT | Wave 94's Phase E shipped clean per spec (boundary acceptance test passed, 1078 tests green); pain concentrated entirely in post-merge smoke + push tail. Phase E `claude in terminal → diff review` was end-to-end broken: 5 cascading bugs in producer/consumer flow (A: payload.cwd absent because hook script never set it — only training data said it did; B: consumer bind-once-on-session_start when terminal-launched claude doesn't emit it reliably; C: stash key collision from random UUID per call, growing unbounded; D: bind-once-per-terminal race blocking second claudes; E: cross-project pathSecurity blocking Gamify diffs in Agent IDE workspace). Lane B B2→B3 cycle ran TWICE — first fix attempt (`3970d6be`) was inert because boundary acceptance test mocked the payload shape we *wished* existed (cwd field), not what Claude Code hook stdin actually emits. Instrumentation in 4 files + sonnet-diagnostician → sonnet-implementer x3 → all fixed in one final bundle (`b7dede57`). Push tail surprise: pre-push `tsc:web` gate found 616 pre-existing TS6307 errors blocking push (renderer→main type cascade through `configTypes`). Sonnet-architect's first surgical fix (add `configTypes.ts` to includes) was wrong — transitive imports unspooled the whole main subsystem. Second architect pass found the actual fix: renderer's `electron-foundation.d.ts` already had `ClaudeCliSettings` but had drifted 2 fields behind main; `useClaudeCliSettings.ts` was importing from main instead of the local copy. Surgical Wave 96 (3 phases, ~10 lines across 6 files) unblocked push. CI minutes exhausted at tag time — tagged from local gates (tsc:web 0, tsc:node 0, lint clean, scoped vitest pass). Filed for next: Wave 97 (full shared-types extraction), 3 diff-review UX follow-ups bumped to Wave 95, pre-push hook should switch to incremental-diff tsc. **Key lesson:** boundary acceptance tests catch contract divergence between sides but DON'T catch when both sides mock the wrong contract — fix is to source mock payloads from real-vendor stdin captures, not from docs/training data. Cost: ~6 hours of "this is 5 minutes from done" recurring. 33 commits up. |
| Lane B B3a + B3b (main-thread hang on context rebuild — algorithmic fix + worker offload) | 2026-05-17 (local; push gated on CI minutes ~2026-06-01) | TEETH-PULLING | Original bug: 2.5-minute UI freeze (152s main-thread block) on every cold context rebuild. **B3a algorithmic fix:** diagnostician's top hypothesis (synchronous SQLite fan-out, ~200 Cypher queries) was **refuted** by per-phase instrumentation re-added in the wave — actual cause was three accidental O(N²) loops over the unfiltered 46,302-file repoIndex in `detectModules` (singleFiles 51s→44ms via `assignCompanionTestFile` dir-bucketing, flatGroups 28s→188ms via sorted+adjacent LCP rewrite of `findPrefixGroups`, featureFolders 7s→244ms via `claimFeatureFolder` bucket-walk). Required parent-file extraction (`repoMapGeneratorSizeCap.ts`, `moduleDetectorSingleFile.ts`) to fit max-lines:300 while adding trace instrumentation. `detectModules` 86s→1s = 82× faster. **B3b worker offload (Phases 0-4):** moved `generateRepoMap` to dedicated worker thread with own read-only `better-sqlite3` connection, mirroring existing `indexingWorker.ts` pattern. Phase 0 acceptance test orchestrator-authored per the orchestrator-owned acceptance-tests rule. WAL multi-reader concurrency verified before plan finalization. Two pre-existing bugs surfaced by Phase 4's improved trace visibility — neither caused by the wave, both filed: (1) `chatStateNewPath` dynamic-require chunker bug from Wave 86 (failed first-attempt static-import fix broke 33 tests because lazy-require was load-bearing for `agentChatThreadStore` module-load side effects; reverted cleanly, post-mortem in bug doc), (2) silent `forceRebuild` hang in `buildRepoIndex` (Phase 3 verification log also lacks the completion line in retrospect; not user-blocking, net-positive vs pre-wave). Mid-wave node_modules damage from a diagnostician's worktree investigation required `npm ci`; using `--ignore-scripts` to skip postinstall blew away the `better-sqlite3` ABI-correct binding and crashed the app on next launch — recovered via `npx electron-rebuild -f --only better-sqlite3` (the `-w` flag is a *filter* and walks the dep tree, `--only` is the working skip-everything-else). 9 commits local |
| W-97 (Shared-Types Extraction) | 2026-05-20 | COOL | 3 work phases over ~1 hour overnight autonomous run. Pure mechanical type-only refactor — least exciting wave in months and that's the point. Phase 0 inventory re-shaped Phase B from W96's "move ~40 orchestration types" estimate to "NO-OP, file follow-up" the moment it became clear those types aren't duplicated (no drift problem) and tsc.web is clean today via explicit `tsconfig.web.json:30-33` includes. Actual W97 scope: 2 CLI-settings interface families (`ClaudeCliSettings` + `CodexCliSettings`). Sonnet-implementer Phase A and Phase C both landed first-try, all gates green. Two distinct ESLint-noUnusedLocals constraints needed the same trick: `export type { X } from '...'` doesn't bring `X` into local scope, so when local code references the moved interface (Phase A: `AgentTemplate.cliOverrides?: Partial<ClaudeCliSettings>`), pattern is `import type` + `export type` both. Phase C surfaced one pre-existing W95 test failure (`mobile-touch-targets` on `WorkbenchRightPane.tsx:50` h-6 button without `/* touch-target-ok */` opt-out) — verified pre-existing by stashing W97 edits and running against pre-W97 HEAD; filed as follow-up. Net diff: ~50 lines moved between files; zero runtime change. Push posture: Cole explicitly authorized push during this autonomous run, overriding the standing 2026-05-19 bulletin posture for one execution. v2.19.0 + v2.19.1 + v2.19.2 + master all pushed at wave-end. CI verification waits for 2026-06-01 minutes-restore.
| W-98 (Orchestration Types Relocation) | 2026-05-20 | COOL | 3 work phases + 1 Tier-1 inline. Mirrors W97 shape: mechanical type-only refactor, no runtime change. Phase 0 inventory caught a 6x scope-estimate error in the source follow-up — file said "~60 types," reality at HEAD was 14 in a single file (the other two named files were already pure re-export shims from a prior wave). Phase A surfaced a structural surprise: destination `src/shared/types/orchestration.ts` was a 3-line barrel, not a content file. Sonnet-implementer handled in-flight by splitting the 14 across `orchestrationProvider.ts` (5 primitives appended) + new `orchestrationApi.ts` (9 IPC-surface types) — exactly the executed form of ADR Decision 1's 300-line-cap contingency clause. Sonnet-phase-reviewer PROCEED on all 4 axes (file-change scope, spec alignment, integrity, runtime viability) — the per-phase review pays for itself when the spec is mechanical but the safety target (`tsc` type identity on IPC surface) is structural. Tier-1 inline doctrine handled the pre-existing touch-target audit failure cleanly: Phase B's `test:renderer` surfaced the W95 residue at `WorkbenchRightPane.tsx:50`, added `/* touch-target-ok */` comment, separate commit so bisect stays clean. Two follow-ups archived (source + touch-target). Net diff: ~+86 lines in shared (14 interfaces appearing) and −4 `tsconfig.web.json` include lines (architectural payoff). Total: ~1.5 hours orchestration including planning. v2.19.3 local; push held per bulletin.
| WB-6 (Workbench overhaul — themes + responsive collapse) | 2026-05-22 (pushed; merge gated on 2026-06-01 CI restore) | COOL | Clean 3-phase renderer-only wave (~one session). Per-theme `workbenchTokens` pipeline + Modern/Warp/Retro canon values + 3-tier responsive collapse via `useWorkbenchBreakpoint`. Two frozen orchestrator-owned guards (theme byte-identity 2/2; responsive acceptance 5/5 — failed 4/5 pre-impl, exactly as designed). Three `sonnet-phase-reviewer` passes; both FLAGs minor + fixed inline (scanline rgba needed the `// hardcoded:` pre-commit suppression; an inert collapse-toggle → wired to actually expand the hunk). `/review` PASS, full suite 11684/0. Process catch: the planned breakpoints (1440/1180) were wrong — once the HUD is dropped, below 1440 is uniformly unified, so the real boundaries are 1760/1440; corrected the ADR+plan before dispatching Phase 3. Minor wrap friction: subagents committed un-prettier'd files (per-phase gate ran lint, not prettier) — formatted at wrap. Check-6 mutation deferred to the batched pre-merge task. (WB-0..WB-5 predate this session; not backfilled per the no-backfill-from-memory rule.) |
