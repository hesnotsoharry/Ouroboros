# Wave 82 — Chat-Only Polish Bundle

## Status

DRAFT · target v2.13.0 · drafted 2026-05-03.

## Context — why this wave exists

A focused user-driven bug-hunt session on 2026-05-03 surfaced 15 distinct defects across the chat-only / workbench surfaces of the Agent IDE. The bugs span four functional areas: (1) chat-only / workbench title-bar wiring (most menu items dispatch DOM events that nothing listens for — `WORKBENCH_NEW_SESSION_EVENT`, `WORKBENCH_NEW_CHAT_EVENT`, `WORKBENCH_OPEN_PROJECT_EVENT`, `WORKBENCH_SWITCH_PROJECT_EVENT` and likely most of View / Tools / Help / Edit Find-Next/Prev have zero `addEventListener` consumers, verified by grep across `src/renderer`); (2) state-management cascades (chat-delete in chat-only view causes a ~0.2s rail flash because the sidebar mutates the Zustand store directly while the workspace's `useThreadState` is unaware — `ChatHistorySidebar.tsx:258-264` vs `agentChatWorkspaceActions.ts:116-135`; outer-rail projects don't persist because `OuterProjectRail.useAddProject` writes only per-window state but not `config.recentProjects`, and chat windows initialize with `projectRoots: []` per `windowManager.ts:244` with no restore path); (3) artifact-pane / file-viewer issues (Recent section auto-populates on every observation, `ArtifactPaneHeader` is redundant with the tab row, edit-mode causes a re-mount that nukes toolbar state and disappears the Edit button, minimap renders alongside the default scrollbar, blame/outline toggles do nothing visible); and (4) context-preview popover gaps (project rules don't load even when `.claude/rules/` is populated, Skills tab shows only executions never available skills, Files tab semantics are unclear, file-tree heat map button is purely cosmetic because `useFileHeatMap.extractFilePath` rejects every JSON tool input).

Many of these are tied to the chat-only / workbench shell that was built incrementally across waves 42-47, 59. Several represent honest follow-ups from those waves (per `roadmap/follow-ups/follow-ups.md`); some are net-new defects from rapid iteration; one (composer typing lag during rule-load bursts) is a perf cascade that touches the shared `AgentEventsContext` so its blast radius extends beyond chat-only. Every bug has been root-caused or scoped to a specific code site through static analysis; only four require runtime profiler / instrumented diagnosis before the fix shape can be locked.

This wave is structurally unusual — not a single-feature build but a coordinated cleanup pass. Phases are organized by **bug class** (mechanical / state / wiring / diagnostic) rather than by build-order, with explicit parallelism across independent surfaces. Phase 0 captures four product-decision items that can't be locked from grounding alone (Recent-section eviction policy, Timeline tab disposition, Files tab semantics, Skills tab data source); the remaining decisions are locked from the user's stated preferences during the bug-hunt session.

## Goal

After this wave, the chat-only / workbench shell behaves as the user expects across the surfaces touched in the 2026-05-03 bug hunt: title-bar menu items either fire their intended action or are removed; deleting a chat causes no rail flash; projects added via the outer rail persist across window restarts; the artifact pane edit-mode toolbar survives entering and exiting edit mode; the file-tree heat map actually colors rows when the agent edits files; the context-preview popover loads project rules reliably and surfaces skills meaningfully; the composer no longer lags during agent rule-load bursts. No new features ship; the wave is purely closing the gap between what the surfaces look like and what they actually do.

## Locked decisions (Phase 0 — ADR)

ADR file: `roadmap/wave-82-DRAFT/wave-82-decisions.md`.

**Locked from grounding (user statements during 2026-05-03 bug hunt):**

1. **Heat map fix path: extend `extractFilePath` to JSON-parse structured tool inputs and read `parsed.file_path` (Write/Edit) or `parsed.notebook_path` (NotebookEdit), falling back to the raw-string path for legacy-shape inputs.** Industry-standard for tool-input parsing in 2026 LLM tooling — every Anthropic / OpenAI tool-use spec since mid-2024 returns structured JSON. The original string-only path is dead for real tool calls.
2. **Branch indicator removal: drop `<GitSection>` mount from `StatusBar.tsx:208`** (or specifically `BranchButton` at line 113); keep `GitBranchIndicator` at top of file tree (`FileTree.tsx:243`). User stated preference for the file-tree variant.
3. **Chat-delete cascade fix: route both `ChatHistorySidebar.handleDelete` and `useWorkbenchRailActions.onDeleteThread` through `model.deleteThread` (the canonical workspace action wired into the store via `useSyncActionsIntoStore`)**, eliminating the direct `applyDeleteToStore` call. The workspace's internal `useThreadState` is the source of truth; the sidebar must use it, not race against it.
4. **Outer-rail persistence quick fix: mirror the IDE-shell add path — when `OuterProjectRail.useAddProject` runs, also write to `config.recentProjects`** (matching `InnerAppLayout.tsx:174-178` and `useProjectManagement.ts:28-35`). Restores the round-trip via the existing fallback merge in `useWorkbenchProjects` at `ChatWorkbenchBody.rails.tsx:37`. Structural fix (per-window roots persisted to sessionsData/SQLite) deferred — see "Out of scope."
5. **Activeproject desync handling: validate `layout.activeProject` against the merged project list on load; if not present, clear it.** One-liner; prevents the inner rail showing a project that's no longer in the outer rail.
6. **Edit-mode label: rename "Exit Edit" to "Exit"** in `FileViewerToolbar.tsx:224`. One-line label change.
7. **Workbench menu wiring strategy: subscribe each unwired event at the `ChatWorkbenchShell` or `TwoTierRailSurface` level** so the listener has access to `layout`, `handlers`, `terminal`, etc. Existing handler implementations (`handleCreateSession`, `model.startNewChat`, `addProjectRoot`, `layout.setActiveProject`) are reused; the wiring is purely subscription, not new logic.
8. **Terminal close affordance: add hover-revealed `TabCloseButton` to `InnerSidebarTerminals` rows AND a "Close session" action button in `ChatWorkbenchTerminalDock` header next to "+ New".** Both bind to `terminal.handleTerminalClose`. The dock header's existing ✕ button stays — it closes the dock as documented.

**REQUIRES USER LOCK (4 items — recommended answers below per `~/.claude/rules/best-practice-spectrum.md`):**

9. **Artifact pane Recent section semantics.**
   - _Industry-standard (recommended pick):_ Recent section caps at 5 chips per row with up to 2 rows (10 total); current open-files row caps at 5; on overflow, oldest open file moves to Recent and the underlying `OpenFile` stays loaded in `FileViewerManager` but is hidden from the active tab strip; clicking a Recent chip moves it back to the open-files row, evicting the oldest open file to Recent (LRU swap). Cap applies to file artifacts only (diffs are transient, surface-once). Source: VS Code's "Recently Opened Editors" pattern (Ctrl+Shift+T) — proven LRU + cap UX.
   - _Emerging:_ Don't unload at all — let `useFileViewerManager` carry unbounded open files but only display 5 in the tab row; Recent is just an alternative view of the rest.
   - _Experimental:_ MRU pinning — user explicitly pins which 5 stay in the open row; Recent shows everything else.
   - REQUIRES USER LOCK.

10. **Timeline tab disposition.**
    - _Industry-standard (recommended pick):_ Lift the cap to ~500 entries (paginated "Load more" affordance for older), keep the tab as-is. Cheapest change; preserves a working surface; counter and visible list stop disagreeing.
    - _Emerging:_ Repurpose Timeline as a digest view (collapsed by session, file-change summaries, milestone markers — not raw events); Monitor stays the raw stream. Differentiates the two surfaces meaningfully.
    - _Experimental:_ Drop Timeline entirely — Monitor covers it.
    - REQUIRES USER LOCK.

11. **Files tab semantics in context-preview popover.**
    - _Industry-standard (recommended pick):_ Unify "Files" and "Mentions" into a single "Context" tab grouped by source (pinned / mentioned / dropped / pasted images / pasted text). Single mental model: "what's going to the prompt." Removes the current confusion (mentions ≠ files ≠ attachments).
    - _Emerging:_ Keep tabs separate but add an "Attachments" tab for images/binaries; Files = pinned only; Mentions = `@`-typed only.
    - _Experimental:_ No tabs at all; flat scrollable list with type badges.
    - REQUIRES USER LOCK.

12. **Skills tab data source.**
    - _Industry-standard (recommended pick):_ Show available skills (the long catalog in the system reminder) as the primary list, with a small "Recently executed" section at the top when `skillExecutions[]` is non-empty. Mirrors the Rules tab's User/Project sub-grouping. Requires a new `rulesAndSkills:listSkills(projectRoot)` IPC analogous to `listRuleFiles`.
    - _Emerging:_ Show only executions but make the empty state actionable ("X skills are available — try /<command>"). Cheapest.
    - _Experimental:_ Defer the tab entirely; remove from popover until skills become a more prominent feature.
    - REQUIRES USER LOCK.

## Scope

**In scope:**

- `src/renderer/components/Layout/ChatOnlyShell/ChatHistorySidebar.tsx` — replace `handleDelete` with `model.deleteThread` invocation; remove `applyDeleteToStore`
- `src/renderer/components/Layout/ChatOnlyShell/useWorkbenchRailActions.ts` — same routing change for the workbench-rail delete path
- `src/renderer/components/Layout/ChatOnlyShell/OuterProjectRail.tsx` — `useAddProject` writes `config.recentProjects` in addition to `addProjectRoot`
- `src/renderer/components/Layout/ChatOnlyShell/ChatWorkbenchBody.rails.tsx` — `useWorkbenchProjects` validates `activeProject` against merged list on render
- `src/renderer/components/Layout/ChatOnlyShell/InnerSidebarTerminals.tsx` — add hover-revealed close button per row
- `src/renderer/components/Layout/ChatOnlyShell/ChatWorkbenchTerminalDock.tsx` — add "Close session" header action next to "+ New"
- `src/renderer/components/Layout/ChatOnlyShell/ChatWorkbenchArtifactPane.tsx` — remove `ArtifactPaneHeader`; redesign artifact-history coordination per Phase 0 decision 9
- `src/renderer/components/Layout/ChatOnlyShell/ArtifactHistoryList.tsx` — switch to horizontal flex-wrap layout (5 chips per row, 2 rows max)
- `src/renderer/components/Layout/ChatOnlyShell/useArtifactHistoryStack.ts` — replace auto-observation with event-driven population (close + overflow)
- `src/renderer/components/Layout/ChatOnlyShell/useWorkbenchTimeline.ts` — implement Phase 0 decision 10
- `src/renderer/components/Layout/ChatOnlyShell/WorkbenchTimelinePanel.tsx` — same
- `src/renderer/components/Layout/ChatOnlyShell/ChatWorkbenchUtilityDrawer.tsx` — remove Timeline tab if Phase 0 decision 10 says drop
- `src/renderer/components/Layout/TitleBar.workbench.menus.ts` — audit dispatched events; for any whose listener is being added in Phase E, leave the dispatch; for any being removed, drop the menu item
- `src/renderer/components/Layout/ChatWorkbenchShell.tsx` (or `TwoTierRailSurface`) — subscribe `WORKBENCH_NEW_SESSION_EVENT`, `WORKBENCH_NEW_CHAT_EVENT`, `WORKBENCH_OPEN_PROJECT_EVENT`, `WORKBENCH_SWITCH_PROJECT_EVENT`, `WORKBENCH_FIND_NEXT_EVENT`, `WORKBENCH_FIND_PREV_EVENT`, plus the View/Tools/Help-menu events identified in the Phase A audit
- `src/renderer/components/FileViewer/FileViewerToolbar.tsx` — change "Exit Edit" → "Exit"; investigate Edit/toolbar disappearance root cause from Phase F
- `src/renderer/components/FileViewer/FileViewer.tsx` and `useFileViewerState.ts` — fix the re-mount / state-reset cascade identified in Phase F
- `src/renderer/components/FileViewer/MonacoEditor.tsx` and `MonacoEditor.hooks.ts` — minimap config: hide vertical scrollbar when minimap is on; investigate edit-mode scroll regression
- `src/renderer/hooks/useFileHeatMap.ts` — fix `extractFilePath` to JSON-parse structured tool inputs
- `src/renderer/components/Layout/StatusBar.tsx` — drop `<GitSection>` mount (per Phase 0 decision 2)
- `src/renderer/hooks/useAgentEvents.ts` and `useAgentEvents.ruleSkillReducers.ts` — coalesce `InstructionsLoaded` events into a single dispatch per microtask burst (perf fix from Phase F diagnosis)
- `src/renderer/hooks/useContextPreview.ts` and `ComposerContextPreview.tsx` — fix project-rules load gap (root cause TBD from Phase F diagnosis); wire Skills tab per Phase 0 decision 12; restructure Files tab per Phase 0 decision 11
- `src/main/agentChat/index.ts` and the `instructions_loaded.mjs` hook script — only modified if Phase F diagnosis traces the project-rule gap to main-side or hook-side
- `src/main/rulesAndSkills/` — add `listSkills` IPC if Phase 0 decision 12 picks the industry-standard option
- `roadmap/follow-ups/outstanding-2026-05-03.md` (new) — categorized digest of genuinely-still-open items from `follow-ups.md` (task #3)
- Manual smoke entry in `roadmap/wave-82-{slug}/wave-82-result.md` per `~/.claude/rules/manual-smoke-gate.md` — chat-only and workbench surfaces are squarely UI-bearing

**Out of scope:**

- Structural per-window `projectRoots` persistence to sessionsData/SQLite — deferred to a future "session model v2" wave; current wave does the quick fix (recentProjects mirror) only
- Lexical migration of any non-composer surface — covered by Wave 81 (composer-only)
- Mobile / touch responsiveness review of any touched surface — separate mobile-responsive wave
- Replacing `useFileHeatMap` decay model with a smarter algorithm — current decay heuristic is fine; we're just unblocking the input parsing
- Refactoring the workbench title-bar menu structure (moving items between menus, renaming categories) — wiring only this wave; restructure if user wants in a later UX-pass wave
- A general performance audit of `AgentEventsContext` re-render churn beyond the rule-load batch fix — if Phase F profiler shows other hot paths, they get filed as follow-ups, not addressed here
- Refactoring `ChatWorkbenchUtilityDrawer` tab structure beyond what Phase 0 decision 10 (Timeline disposition) requires
- Adding new artifact types (images, terminals, settings) to the artifact pane — Recent redesign covers files/diffs only

## Phases

| Phase | Topic                                        | Implementer                      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----- | -------------------------------------------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | ADR / locked decisions                       | orchestrator                     | 12 numbered decisions in `wave-82-decisions.md`. Decisions 1-8 in abbreviated `Context / Pick / Rationale` form (locked from user-stated grounding). Decisions 9-12 in full best-practice-spectrum form per `~/.claude/rules/best-practice-spectrum.md`, marked `REQUIRES USER LOCK`. Phase A blocked until decisions 9-12 are locked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| A     | Architect pass — bug-bundle dispatch matrix  | sonnet-architect                 | Read-only deliverable to `roadmap/wave-82-{slug}/phase-a-audit.md`. Three artifacts: (1) **Workbench title-bar wiring matrix** — for every menu item in `TitleBar.workbench.menus.ts`, document the dispatched event, whether a listener exists today (grep `addEventListener` for the constant), and the recommended subscription target if missing. Output as a table the implementer can act on mechanically. (2) **Bug dependency graph** — show which bugs share root causes (e.g., #9 edit-mode bugs likely all one re-mount cascade) and which can run in parallel. (3) **Sequence diagram for the chat-delete cascade** showing current race between `applyDeleteToStore` → store re-render → `useSyncStateIntoStore` → stale `model.threads` re-introduces the row, vs. the post-fix path through `model.deleteThread` → `useThreadState.setThreads` → store sync. **Diagram terminus per Site 1: chat-only sidebar visibly removes the deleted row in a live IDE chat-only window with zero visible flash on the inner rail** — a user-observable endpoint Cole confirms by deleting a chat in the dev build.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B     | Mechanical micro-fixes batch                 | haiku-implementer × 5 (parallel) | Five independent single-file edits, each ≤30 lines. Dispatch in parallel via `sonnet-batch-coordinator`-produced plan. (B1) `FileViewerToolbar.tsx:224` — `'Exit Edit'` → `'Exit'`. (B2) `useFileHeatMap.ts:91-98` — extend `extractFilePath` to JSON-parse and read `parsed.file_path` / `parsed.notebook_path`; preserve raw-string fallback. (B3) `StatusBar.tsx` — remove `<GitSection>` mount per Phase 0 decision 2. (B4) `InnerSidebarTerminals.tsx` — add hover-revealed `TabCloseButton` per row + `ChatWorkbenchTerminalDock.tsx` — add "Close session" header action. (B5) `InnerSidebar.tsx:160-169` — delete `SidebarFooter` component and its render call (purely decorative "Workspace" label with no content beneath, no purpose). Each worker writes to its assigned file only; orchestrator merges and runs `npx vitest run` on each touched test file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| C     | State persistence fixes                      | sonnet-implementer               | Three coordinated edits in chat-only state surfaces. (C1) Replace `ChatHistorySidebar.handleDelete` (lines 258-264) with a call to `model.deleteThread` read from `useAgentChatStoreContext`; delete `applyDeleteToStore` (lines 233-239). Same routing change in `useWorkbenchRailActions.ts:onDeleteThread` (lines 71-84). (C2) `OuterProjectRail.useAddProject` (lines 240-249) — after `addProjectRoot(path)`, also call `window.electronAPI.config.set('recentProjects', updated)` mirroring `InnerAppLayout.tsx:174-178`. (C3) `useWorkbenchProjects` in `ChatWorkbenchBody.rails.tsx:31-45` — after building `merged`, validate `layout.activeProject`; if not in `merged`, call `layout.setActiveProject(null)`. Tests: chat-delete integration (no flash assertion via render-count probe), outer-rail add-then-restart persistence (mock `setProjectRoots` + `setConfig`), activeproject-desync clearing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D     | Workbench title-bar wiring sweep             | sonnet-implementer               | Implements the wiring matrix from Phase A. Subscribe each unwired event at the appropriate level (most at `ChatWorkbenchShell` or `TwoTierRailSurface`; Edit's Find Next/Prev at the active chat conversation surface). For events whose intended action has no existing handler implementation, surface that in a written addendum to the audit and confer with orchestrator before fabricating new functionality (most should map to existing handlers per Phase 0 decision 7). For menu items whose action is genuinely deprecated or out of scope, remove the menu entry rather than wire a no-op. Tests: per-event listener mount + dispatch + handler invocation (mock the handler, fire the event, assert).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| E     | Diagnostic sprint (no code changes)          | sonnet-diagnostician             | Internal — produces `roadmap/wave-82-{slug}/phase-e-diagnosis.md`. Four diagnostic threads, each with instrumented logs and a written root-cause finding. (E1) **Edit-mode bugs (#9):** add `console.time` markers around `useFileViewerState` instance identity, `EditControls` `onSave` value, `ContentRouter` routing decision; reproduce edit→exit cycle and capture which value flips that breaks the toolbar. Hypothesis to verify or rule out: phantom `filePath` change triggers `useFileViewerState` reset + `FileViewer` re-mount via key change. (E2) **Project rules don't load (#6):** instrument `instructions_loaded.mjs` hook (log every event with memoryType + filePath) and `useAgentEvents.ruleSkillReducers` rule-add path; spawn a fresh chat in Agent IDE; compare emitted events against the 9 known project rules in `.claude/rules/`. Find the gap layer. (E3) **Composer typing lag (#15):** add `console.time` markers around `useAgentEvents` reducer dispatch, `useSyncStateIntoStore` effect, `LexicalChatComposer` render. Reproduce by typing during a session spawn that loads ≥10 rules. Confirm via React DevTools profiler whether composer re-renders on each rule-load. Identify the cheapest fix point (event batching vs. context-selector narrowing vs. model-ref stabilization vs. defer-sync). (E4) **"File > New Session froze IDE" symptom (#13):** instrument the dropdown portal dismiss path + the `WORKBENCH_NEW_SESSION_EVENT` dispatch site; reproduce; capture stack at freeze. Likely a focus trap or portal that doesn't dismiss after click. **Internal — no observation point** (deliverable is the written diagnosis, not user-visible code change). |
| F     | Diagnostic-driven implementation             | sonnet-implementer               | Implements the four fixes the Phase E diagnosis pinpoints. F1: edit-mode re-mount fix (probably tightening a `key=` prop or memoizing a state factory). F2: project-rules fix (location depends on E2 — could be hook-script bug, main-side handler, or renderer-side reducer). F3: composer-lag fix (most likely InstructionsLoaded coalescing in `useAgentEvents.ruleSkillReducers.ts` — debounce a rule-add burst into a single dispatch; secondary fix points held in reserve). F4: freeze fix (likely a portal `onClose` not firing — patch the dropdown component or escape-handler). Each fix lands with a regression test matching the Phase E reproduction case.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| G     | Artifact pane redesign                       | sonnet-implementer               | Implements Phase 0 decision 9. (G1) `ChatWorkbenchArtifactPane.tsx` — delete `ArtifactPaneHeader` component (lines 16-41) and its usage (line 156). The pane's Close button moves to the artifact-history list header or to the FileViewerTabs row's right edge — match Phase A audit recommendation. (G2) `ArtifactHistoryList.tsx` — replace vertical `flex flex-col` (line 53) with `flex flex-wrap` (5 chips per row, 2 rows max via `max-h-[3.5rem] overflow-hidden`); chip width sized for 5-per-row at typical pane widths. (G3) `useArtifactHistoryStack.ts` — delete `useArtifactObserver` (lines 79-99); replace with explicit event subscriptions: `useFileViewerManager` `onClose` event (push closed file to recent), and an overflow check inside `useFileViewerManager` when `openFiles.length > 5` (evict oldest to recent). Recent-chip click moves the entry back to open and evicts the current oldest. Tests: open-then-close → appears in Recent; open 6th → 1st moves to Recent; click Recent → swap LRU. (G4) Edit-mode toolbar fixes from Phase F also land here as a coordinated artifact-pane pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| H     | Context popover work                         | sonnet-implementer               | Implements Phase 0 decisions 11 (Files tab) and 12 (Skills tab). (H1) Skills tab: if industry-standard pick, add `rulesAndSkills:listSkills(projectRoot)` IPC in `src/main/rulesAndSkills/` returning `{ name, scope, source }[]`; wire `ComposerContextPreview` to fetch and pass to `useContextPreview` as `availableSkills`; restructure `ContextPreview.tsx` Skills tab to show available list with "Recently executed" subsection at top when `skillExecutions[]` non-empty. (H2) Files tab: if industry-standard "unify" pick, merge Files + Mentions into a single "Context" tab grouped by source; update `TABS` array in `ContextPreview.tsx:24-32` and `EMPTY_TAB_MESSAGES`. Add an "Attachments" group surfacing `attachments[]` from the workspace store (currently invisible in popover). External non-image drop fix: extend `imageAttachmentSupport.handleDrop` to handle non-image files by pinning as a context entry (calls `useAgentChatContext.addFile`); preserve image-only path for the existing image case. Tests: list-skills IPC contract + tab render; unified Context tab grouping; non-image drop pins as file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| I     | Documentation: outstanding follow-ups digest | haiku-research-extractor         | Reads `roadmap/follow-ups/follow-ups.md` (which already has 45 items triaged) plus `roadmap/audit-verification-pass.md` (referenced as authoritative tracker). Produces `roadmap/follow-ups/outstanding-2026-05-03.md` with a categorized digest of genuinely-still-open items (the "71 NOT-DONE + 8 PARTIAL" mentioned at line 91 of follow-ups.md). Categories: chat/UI, telemetry, MCP, graph, codebase memory, performance, deferred-by-design. Each entry: one-line summary + source pointer (`<file>:<line>`). Output is a planning document for future waves, not a wave deliverable that ships to users. **Can run in parallel with all other phases — no shared files.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| J     | Wave wrap + manual smoke                     | orchestrator                     | Full vitest suite, lint, typecheck, formatter. `/review 82` mechanical gap-check. Manual smoke checklist in `wave-82-result.md` per `~/.claude/rules/manual-smoke-gate.md` covering EVERY surface this wave touched: chat-only delete-no-flash, outer-rail add-then-restart persistence, terminal close in chat-only, artifact pane Recent behavior, edit-mode toolbar survival, heat map color rendering after agent edit, every wired menu item firing its action, no project-rules-empty popover state, composer typing during rule-load burst with no perceptible lag. Result brief at `roadmap/wave-82-{slug}/wave-82-result.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### Phase ordering

```
0 (ADR — blocks on user lock of decisions 9-12)
   │
   ▼
A (architect pass — produces dispatch matrix + bug dependency graph)
   │
   ├──► B (mechanical batch — 4 parallel haiku workers)
   ├──► C (state persistence)
   ├──► D (menu wiring sweep)
   ├──► E (diagnostic sprint — produces phase-e-diagnosis.md)
   │     │
   │     ▼
   │     F (diagnostic-driven implementation)
   │
   ├──► G (artifact pane — depends on Phase 0 decision 9 lock + can integrate Phase F edit-mode fix)
   │
   ├──► H (context popover — depends on Phase 0 decisions 11 & 12)
   │
   └──► I (docs digest — fully parallel, no shared files)

   All converge ──► J (wrap + smoke + result brief)
```

Phase 0 must complete before any other phase starts (locks decisions 9-12 needed by G + H). Phase A produces the wiring matrix that D consumes and the bug-dependency graph that informs E's diagnosis order. Phases B, C, D, E, G, H, I can run in parallel after A; G integrates the Phase F edit-mode fix, so G and F have a soft dependency (G can start the artifact-pane redesign work while F runs, but G can't merge until F's edit-mode patch is in). J is gated on every other phase reporting complete + manual smoke signed.

## Risks

| Risk                                                                                                                                                                                                                                                         | Mitigation                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cole doesn't lock decisions 9-12 promptly, blocking G + H                                                                                                                                                                                                    | Phase 0 dispatched first thing in the wave; Phase A can run during the wait if user lock is delayed >24h. B/C/D/E/F/I are not blocked by 9-12 — let them run. Re-converge at J.                                                                                                          |
| Phase E diagnosis points to a fix path that's significantly larger than estimated (e.g., composer lag requires replacing `AgentEventsContext` with selector-based subscription model, not a simple debounce)                                                 | Phase F dispatch is gated on Phase E's recommendation. If F's scope balloons past ~150 lines or touches >3 files outside the bug's root area, surface to orchestrator: defer to a follow-up wave with a documented filed item. Do not silently expand wave scope.                        |
| Wiring sweep (Phase D) discovers menu items whose intended action genuinely has no existing handler (i.e., the feature was never built)                                                                                                                      | Phase A audit flags these explicitly. Phase D's per-event addendum confers with orchestrator before fabricating: prefer to remove the menu entry over wiring a no-op or building new functionality outside this wave's scope.                                                            |
| Phase B mechanical fixes pass tests in isolation but break a tested integration (e.g., `extractFilePath` JSON parse change disrupts a downstream consumer of the heat map data)                                                                              | Each B-worker runs its scoped vitest script (`test:filetree`, `test:renderer`, etc.) before reporting complete. Orchestrator runs `npm run test:renderer` post-merge. Phase J full-suite catches anything else.                                                                          |
| Phase F's composer-lag fix (rule-load coalescing) introduces a new race: if a rule loads after the coalesce window closes, it lands in the next dispatch with a stale view of state                                                                          | Coalesce timer is microtask-scoped (`queueMicrotask`), so all events from a single Claude Code stream-json batch land in one dispatch. If a stream is genuinely interleaved with other state changes, that's the same race the existing code has — no regression. Tests assert ordering. |
| Phase G's Recent-section eviction model (Phase 0 decision 9 industry-standard pick) has UX edge cases not surfaced by the spectrum analysis (e.g., user opens file 6, file 1 evicts to Recent, user immediately closes file 6 expecting to return to file 1) | Phase G's regression tests cover the LRU swap explicitly. If the manual smoke (Phase J) surfaces a UX surprise, file as a follow-up rather than redesigning mid-wave.                                                                                                                    |
| Heat map fix (B2) starts populating data that downstream consumers don't expect (e.g., a row count that affects layout)                                                                                                                                      | Heat map is purely visual (left-border color); no consumers downstream of the data shape itself. Verify by grep: `getHeatLevel` consumers in `FileTreeBody.tsx` and child components — confirm none depend on the data being empty.                                                      |
| Workbench menu wiring (Phase D) accidentally creates an event-listener duplicate when the workbench shell remounts (e.g., user toggles immersive chat off-and-on)                                                                                            | Subscriptions use `useEffect` cleanup; Phase D's tests assert listener count after mount/unmount cycles. Manual smoke (Phase J) tests immersive-toggle once.                                                                                                                             |
| Outer-rail persistence fix (C2) — writing to `recentProjects` from the chat-only add path could create a duplicate entry if the project is also in per-window roots                                                                                          | `recentProjects` already deduplicates via `[path, ...recentProjects.filter((p) => p !== path)].slice(0, 10)` pattern. Mirror that exactly.                                                                                                                                               |
| Manual smoke (Phase J) requires Cole to walk through all touched surfaces — risk of skipping a surface and shipping a regression                                                                                                                             | Phase J checklist explicitly enumerates every surface (~12 items). `~/.claude/rules/manual-smoke-gate.md` rule is non-negotiable.                                                                                                                                                        |
| Phase I (follow-ups digest) is parallel and could be forgotten if it stalls                                                                                                                                                                                  | I is small (≤200 lines). Orchestrator includes I status in the J convergence check.                                                                                                                                                                                                      |

## Test coverage by phase

| Phase | Unit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Integration                                                                                                                                                                                                                                         | Notes                                                                                                                                  |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | n/a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | n/a                                                                                                                                                                                                                                                 | ADR is a written decision document, not code.                                                                                          |
| A     | n/a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | n/a                                                                                                                                                                                                                                                 | Read-only architect deliverable; no test surface. Audit document is verified by reading.                                               |
| B1    | `FileViewerToolbar.test.tsx` — assert button label flips correctly between "Exit" and "Edit" by edit-mode state.                                                                                                                                                                                                                                                                                                                                                                                               | n/a                                                                                                                                                                                                                                                 | Trivial — preserves existing test except for the label string.                                                                         |
| B2    | `useFileHeatMap.test.ts` — JSON-parse path returns correct file path for `{"file_path": "..."}`, `{"notebook_path": "..."}`, raw-string fallback for legacy.                                                                                                                                                                                                                                                                                                                                                   | n/a (Phase J manual smoke covers visual heat-map display).                                                                                                                                                                                          | Pure parser change; covered by unit tests.                                                                                             |
| B3    | `StatusBar.test.tsx` — assert no `BranchButton` renders.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | n/a                                                                                                                                                                                                                                                 | Snapshot diff if the existing test uses one; otherwise direct query.                                                                   |
| B4    | `InnerSidebarTerminals.test.tsx` — hover state reveals close button; click invokes `onClose` with session id. `ChatWorkbenchTerminalDock.test.tsx` — header "Close session" button invokes `terminal.handleTerminalClose(activeSessionId)`.                                                                                                                                                                                                                                                                    | n/a                                                                                                                                                                                                                                                 | Hover state is the slightly tricky part — use `userEvent.hover`.                                                                       |
| B5    | `InnerSidebar.test.tsx` — assert no element with `data-testid="inner-sidebar-footer"` renders.                                                                                                                                                                                                                                                                                                                                                                                                                 | n/a                                                                                                                                                                                                                                                 | Single negative assertion.                                                                                                             |
| C     | `ChatHistorySidebar.test.tsx` — delete invocation routes to `model.deleteThread`, NOT to `applyDeleteToStore`. `useWorkbenchRailActions.test.ts` — same routing assertion. `OuterProjectRail.test.tsx` — `useAddProject` calls both `addProjectRoot` AND `window.electronAPI.config.set('recentProjects', ...)`. `ChatWorkbenchBody.rails.test.tsx` — when `activeProject` not in merged list, `setActiveProject(null)` is called.                                                                             | `chatOnlyDeleteNoFlash.integration.test.tsx` (new) — delete a chat in a mounted chat-only shell; assert the rail's render count between delete and final state is exactly 1 (no flash = no intermediate render with the deleted row re-introduced). | The render-count probe is the closest test surface to the user's "no flash" experience. Manual smoke is the experiential verification. |
| D     | `TitleBar.workbench.menus.test.ts` — assert each newly-wired event has a corresponding listener mounted by the workbench shell on render. Per-event handler invocation tests (mock the handler, fire the event, assert called).                                                                                                                                                                                                                                                                                | `ChatWorkbenchShell.menuWiring.integration.test.tsx` (new) — render shell, fire each menu event, assert the corresponding handler is invoked.                                                                                                       | Coverage by event count — every event in the audit matrix gets a row.                                                                  |
| E     | n/a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | n/a                                                                                                                                                                                                                                                 | Diagnostic phase produces a written document, not code.                                                                                |
| F     | Per-fix unit tests matching the Phase E reproduction case. F1: edit-mode re-mount regression test (assert `useFileViewerState` instance identity persists across edit→exit). F2: project-rules load test (mock InstructionsLoaded events from the corrected hook path; assert reducer adds them). F3: composer-lag coalesce test (fire 16 InstructionsLoaded events synchronously; assert exactly 1 reducer dispatch). F4: dropdown freeze test (open menu, click item, assert portal dismisses within frame). | `chatOnlyEditMode.integration.test.tsx`, `composerRuleLoadPerf.integration.test.tsx`                                                                                                                                                                | Coverage tracks the four diagnostic threads; F2 may also touch main-side test if the gap is in the hook script.                        |
| G     | `ChatWorkbenchArtifactPane.test.tsx` — `ArtifactPaneHeader` no longer renders. `ArtifactHistoryList.test.tsx` — horizontal flex-wrap layout; assert max 10 chips visible. `useArtifactHistoryStack.test.ts` — close pushes to recent; 6th open evicts 1st to recent; recent-click swaps LRU.                                                                                                                                                                                                                   | `chatWorkbenchArtifactRecent.integration.test.tsx` (new) — full pane mount; open 6 files; assert tab row shows 5; assert Recent shows 1; click Recent chip; assert swap.                                                                            | Edit-mode tests from F1 also pass through here.                                                                                        |
| H     | `useContextPreview.test.ts` — `availableSkills` input populates Skills tab items. `ContextPreview.test.tsx` — Skills tab shows "Recently executed" subsection when `skillExecutions[]` non-empty; unified Context tab groups by source if Phase 0 decision 11 picks unify. `imageAttachmentSupport.test.ts` — non-image drop calls `addFile` (not `handleFiles`).                                                                                                                                              | `composerContextPopover.integration.test.tsx` (new) — Skills tab populated from mock IPC; non-image file drop appears in Files/Context group.                                                                                                       | If Phase 0 decision 12 picks "executions only" or "defer," skip the IPC test.                                                          |
| I     | n/a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | n/a                                                                                                                                                                                                                                                 | Documentation-only deliverable.                                                                                                        |
| J     | n/a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Full vitest suite + lint + typecheck + `/review 82`.                                                                                                                                                                                                | Manual smoke checklist signed in `wave-82-result.md`.                                                                              |

## Acceptance criteria

- [ ] `roadmap/wave-82-{slug}/wave-82-decisions.md` exists with 12 numbered decisions; decisions 1-8 in `Context / Pick / Rationale` form; decisions 9-12 in full best-practice-spectrum form with Cole's lock recorded.
- [ ] `roadmap/wave-82-{slug}/phase-a-audit.md` exists with three sections: workbench title-bar wiring matrix (every menu event mapped to listener-exists / recommended-target), bug dependency graph, chat-delete sequence diagram with terminus stated.
- [ ] `roadmap/wave-82-{slug}/phase-e-diagnosis.md` exists with root-cause findings for #6 (project rules), #9 (edit-mode), #13-freeze, and #15 (composer lag); each finding cites instrumented evidence (log captures, profiler screenshots, or stack traces).
- [ ] `FileViewerToolbar.tsx:224` reads `props.editMode ? 'Exit' : 'Edit'`.
- [ ] `useFileHeatMap.extractFilePath` returns a non-null path for input `'{"file_path": "/foo.ts"}'` (verified by unit test).
- [ ] `StatusBar.tsx` no longer mounts `<GitSection>` (verified by grep + unit test snapshot).
- [ ] `InnerSidebarTerminals.tsx` rows expose a hover-revealed close button bound to `terminal.handleTerminalClose`.
- [ ] `ChatWorkbenchTerminalDock.tsx` header has a "Close session" button next to "+ New" bound to `terminal.handleTerminalClose(activeSessionId)`.
- [ ] `ChatHistorySidebar.handleDelete` invokes `model.deleteThread` (not `applyDeleteToStore`); same for `useWorkbenchRailActions.onDeleteThread`.
- [ ] `OuterProjectRail.useAddProject` calls `window.electronAPI.config.set('recentProjects', ...)` after `addProjectRoot`.
- [ ] `useWorkbenchProjects` validates `layout.activeProject` against the merged project list and clears it via `layout.setActiveProject(null)` if not present.
- [ ] Every event constant defined in `appEventNames.ts` and dispatched from `TitleBar.workbench.menus.ts` has at least one `addEventListener` consumer in `src/renderer` (verified by grep). Menu items whose event is intentionally removed are deleted from the menu definition, not left as no-op dispatchers.
- [ ] In a live chat-only window, deleting a chat removes the row from the inner rail in a single render cycle with no visible flash (Cole confirms manually; integration test asserts render count = 1).
- [ ] In a live chat-only window, adding "Contractor App" via the outer rail's "+" button, restarting the app, and reopening: Contractor App still appears in the outer rail.
- [ ] In a live chat-only window with the workbench variant on, every menu item (File / Edit / View / Tools / Help) either fires its intended action when clicked OR has been removed from the menu (no silent no-ops).
- [ ] In a live chat-only window with the workbench variant on, opening 6 files in the artifact pane shows 5 in the active tab row and 1 in the Recent section; closing a file moves it to Recent; clicking a Recent chip swaps it back.
- [ ] In a live IDE window, after agent edits at least 2 files, the file tree displays colored left-borders on those rows reflecting heat level.
- [ ] In a live chat-only window, opening the context-preview popover after a fresh chat session in Agent IDE shows project rules under the Project sub-tab (not 0).
- [ ] In a live chat-only window, typing in the composer during a fresh-session rule-load burst (≥10 rules loading) produces no visible character lag — keystrokes commit at typing speed without batching.
- [ ] In a live IDE window, entering edit mode in the artifact pane and pressing Exit returns the toolbar to its pre-edit state — Edit / Minimap / Blame / Outline / History buttons all remain present and responsive without requiring close-and-reopen.
- [ ] `roadmap/follow-ups/outstanding-2026-05-03.md` exists with categorized digest of genuinely-still-open items.
- [ ] `npm run build` succeeds; `npx tsc --noEmit` clean; full vitest suite green; manual smoke checklist in `wave-82-result.md` signed.

## Verification

### Per-phase experiential observation

The data-shape probes below confirm the JSON / file-on-disk populates correctly. They do NOT confirm the user observes anything different — that's what this table is for. Each row anchors a phase to a concrete user-facing surface and the full path from change site to observation. See `~/.claude/notes/wave-process.md` "Site 2" for the rule.

| Phase | Observation point                                                                     | Path to it                                                                                                                                                                                                                                                                                                                                            | What "working" looks like there                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | Internal — no observation point                                                       | n/a                                                                                                                                                                                                                                                                                                                                                   | Phase 0 produces a written ADR document. The decisions inside govern subsequent phases and are validated through their downstream phase's observation rows.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A     | Internal — no observation point                                                       | n/a                                                                                                                                                                                                                                                                                                                                                   | Phase A is a read-only architect-pass deliverable producing `phase-a-audit.md`. The wiring matrix and bug dependency graph are inputs to D, E, F, G; their correctness is validated through those phases' observation rows. The chat-delete sequence diagram's terminus is bound to Site 1's user-observable-terminus rule, with the terminus stated in this Phases table Notes column (chat-only sidebar visibly removing the deleted row with no flash).                                                                                                                                                                                                                                     |
| B1    | Edit toolbar in Cole's live IDE artifact pane                                         | Cole opens a file in the artifact pane → toggles into edit mode → reads the toolbar button label                                                                                                                                                                                                                                                      | Cole sees the button labeled "Exit" instead of "Exit Edit"; clicking it returns to read-only view as before.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B2    | File tree rows in Cole's live IDE window after agent activity                         | Cole prompts the agent to edit 2-3 files → InstructionsLoaded / tool-call events flow through `useAgentEvents` → `useFileHeatMap` records edits via JSON-parsed file paths → `getHeatLevel(path)` returns non-undefined → FileTreeItem renders left-border color                                                                                      | Cole sees colored left-borders (warm/hot/fire) appear on the recently-edited file rows in the file tree, fading over time per the decay heuristic. Heat map button toggle ON/OFF visibly toggles the borders on/off.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| B3    | Status bar bottom-left in Cole's live IDE window                                      | Cole launches the app → reads the status bar                                                                                                                                                                                                                                                                                                          | Cole sees the branch indicator no longer appears in the bottom-left status bar; the branch indicator at the top of the file tree is the sole branch readout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B4    | Inner sidebar Terminals tab + workbench terminal dock in Cole's live chat-only window | Cole opens chat-only workbench → inner sidebar Terminals tab → spawns a terminal → hovers a row                                                                                                                                                                                                                                                       | Cole sees an "✕" appear on the right side of the terminal row on hover; clicking it removes the terminal from the list AND from the dock (if active). Separately, in the dock header, Cole sees a "Close session" button next to "+ New" that closes the active terminal.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| B5    | Bottom of inner sidebar in Cole's live chat-only workbench window                     | Cole opens chat-only workbench → looks at the bottom of the inner sidebar                                                                                                                                                                                                                                                                             | Cole sees the static "Workspace" label and its containing footer strip are gone; the inner sidebar's content extends to the bottom edge with no decorative footer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C     | Chat-only inner rail + outer rail in Cole's live chat-only window                     | (delete path) Cole opens a chat in chat-only → right-clicks → Delete → observes the inner rail. (persistence path) Cole adds Contractor App via outer rail "+" → quits app → relaunches → opens chat-only → observes outer rail. (desync path) Cole removes a project via outer rail context menu while it's the active project → observes inner rail | (delete) Cole sees the deleted chat row vanish in a single visual update with no flicker, no momentary re-appearance, no rail re-render. (persistence) Cole sees Contractor App still present in the outer rail after relaunch. (desync) Cole sees the inner rail's "active project" state cleared (no orphan reference) when the project is removed from the outer rail.                                                                                                                                                                                                                                                                                                                      |
| D     | Chat-only / workbench title bar menus in Cole's live chat-only window                 | Cole opens chat-only workbench → clicks each menu (File / Edit / View / Tools / Help) → clicks each item                                                                                                                                                                                                                                              | Cole sees every menu item either perform a visible action (new chat appears, project picker opens, panel toggles, find bar opens) OR has been removed from the menu (item no longer present). No item silently does nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| E     | Internal — no observation point                                                       | n/a                                                                                                                                                                                                                                                                                                                                                   | Phase E produces `phase-e-diagnosis.md`. Findings are written conclusions with instrumented evidence; no user-facing change ships from this phase. The fixes in Phase F deliver the user-observable behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F     | (varies per fix — see individual rows)                                                | (per fix)                                                                                                                                                                                                                                                                                                                                             | (F1) Cole enters edit mode in the artifact pane, presses Exit, and observes the Edit / Minimap / Blame / Outline / History buttons all remain present and clickable without re-opening the file. (F2) Cole opens the context-preview popover in a fresh Agent IDE chat-only session and observes project rules under the Project sub-tab (count > 0, names match `.claude/rules/*.md`). (F3) Cole types continuously in the composer during a fresh chat-session spawn that loads ≥10 rules; the typed characters appear at typing speed with no perceptible batching, lag, or stutter. (F4) Cole clicks File > New Session and observes the menu dropdown dismiss cleanly with no IDE freeze. |
| G     | Artifact pane in Cole's live chat-only workbench window                               | Cole opens 6 files in the artifact pane → observes the active tab row and Recent section                                                                                                                                                                                                                                                              | Cole sees the active tab row showing 5 files (the most recently opened) and the Recent section showing 1 chip (the oldest). Closing a file moves it to Recent. Clicking a Recent chip moves it back to the active tab row, and the current oldest moves to Recent. The redundant "Editor" header row no longer appears above the tab row.                                                                                                                                                                                                                                                                                                                                                      |
| H     | Context preview popover in Cole's live chat-only window                               | Cole opens the popover → switches between tabs (Skills, Files / Context)                                                                                                                                                                                                                                                                              | (Skills) Cole sees a list of available skills (the catalog from the system reminder), with a "Recently executed" subsection at the top if any have run this session — the empty-state "No skills executed in this session" no longer appears as the only content. (Files) If unify chosen: Cole sees a single Context tab grouped by source (Pinned / Mentioned / Dropped / Attachments) — dropping a non-image file from the OS file explorer adds it under "Dropped" (or equivalent group); pasted images appear under "Attachments."                                                                                                                                                        |
| I     | Internal — no observation point                                                       | n/a                                                                                                                                                                                                                                                                                                                                                   | Phase I produces `outstanding-2026-05-03.md` — a planning document for future waves, not a user-shipping artifact. Its correctness is validated by Cole reading it and confirming the categorization matches expectations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| J     | Every surface from B-H in Cole's live IDE window                                      | Cole walks the manual smoke checklist in `wave-82-result.md` end-to-end                                                                                                                                                                                                                                                                           | Each of the 12 acceptance-criteria observation points (above) confirmed by Cole on a freshly-built dev instance; signed checklist entry in the result brief.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

### Data-shape probes

```bash
# After Phase B1 — Exit label
grep "'Exit'" src/renderer/components/FileViewer/FileViewerToolbar.tsx
# Expected: at least 1 line including the label-toggle ternary

# After Phase B2 — heat map JSON parse
grep -A 3 "extractFilePath" src/renderer/hooks/useFileHeatMap.ts | grep "JSON.parse\|file_path"
# Expected: the helper now references JSON.parse and parsed.file_path

# After Phase B3 — branch button gone
grep "BranchButton\|GitSection" src/renderer/components/Layout/StatusBar.tsx
# Expected: empty

# After Phase B4 — terminal close affordance
grep "TabCloseButton\|handleTerminalClose" src/renderer/components/Layout/ChatOnlyShell/InnerSidebarTerminals.tsx src/renderer/components/Layout/ChatOnlyShell/ChatWorkbenchTerminalDock.tsx
# Expected: ≥2 hits across both files

# After Phase C — chat delete cascade routing
grep "applyDeleteToStore" src/renderer/components/Layout/ChatOnlyShell/ChatHistorySidebar.tsx
# Expected: empty (function deleted)
grep "model.deleteThread\|model\\?.deleteThread" src/renderer/components/Layout/ChatOnlyShell/ChatHistorySidebar.tsx src/renderer/components/Layout/ChatOnlyShell/useWorkbenchRailActions.ts
# Expected: ≥2 hits

# After Phase C — outer-rail recentProjects mirror
grep "recentProjects" src/renderer/components/Layout/ChatOnlyShell/OuterProjectRail.tsx
# Expected: ≥1 hit (set call inside useAddProject)

# After Phase D — every workbench event has a listener
for evt in WORKBENCH_NEW_SESSION_EVENT WORKBENCH_NEW_CHAT_EVENT WORKBENCH_OPEN_PROJECT_EVENT WORKBENCH_SWITCH_PROJECT_EVENT WORKBENCH_FIND_NEXT_EVENT WORKBENCH_FIND_PREV_EVENT; do
  count=$(grep -r "addEventListener.*$evt\|window.addEventListener.*$evt" src/renderer | wc -l)
  echo "$evt: $count listener(s)"
done
# Expected: each event ≥1

# After Phase G — artifact pane header gone
grep "ArtifactPaneHeader" src/renderer/components/Layout/ChatOnlyShell/ChatWorkbenchArtifactPane.tsx
# Expected: empty (component removed)

# After Phase H — listSkills IPC (if industry-standard pick)
grep -r "listSkills\|rulesAndSkills:listSkills" src/main src/preload src/renderer/types
# Expected: ≥3 hits (handler + preload bridge + type def) IF Phase 0 decision 12 picked industry-standard

# After Phase J — full quality gates
npx tsc --noEmit                                                         # exit 0
npm run lint                                                              # exit 0
timeout 360 npx vitest run                                                # exit 0
```

## Files the next agent should read first

1. `roadmap/wave-82-{slug}/wave-82-decisions.md` — locked decisions; required reading before any phase starts implementation.
2. `roadmap/wave-82-{slug}/phase-a-audit.md` — wiring matrix + bug dependency graph + sequence diagram (produced by Phase A; required for D, E, F, G).
3. `roadmap/wave-82-{slug}/phase-e-diagnosis.md` — diagnostic findings (produced by Phase E; required for F).
4. `src/renderer/components/Layout/ChatOnlyShell/CLAUDE.md` — chat-only / workbench shell architecture overview; the surface most heavily touched by this wave.
5. `src/renderer/components/AgentChat/CLAUDE.md` — composer + workspace store architecture; relevant to phases C, D, F (composer lag), H.
6. `src/renderer/components/FileTree/CLAUDE.md` — file tree + heat map + branch indicator architecture; relevant to phases B2, B3.
7. `src/renderer/components/FileViewer/CLAUDE.md` — file viewer + edit-mode architecture; relevant to phases B1, F (edit-mode), G.
8. `src/renderer/hooks/useFileHeatMap.ts` — heat map source-of-truth (Phase B2 fix site).
9. `src/renderer/components/Layout/ChatOnlyShell/ChatHistorySidebar.tsx` and `useWorkbenchRailActions.ts` — chat-delete fix sites (Phase C).
10. `src/renderer/components/Layout/ChatOnlyShell/OuterProjectRail.tsx` and `ChatWorkbenchBody.rails.tsx` — project persistence + activeproject desync fix sites (Phase C).
11. `src/renderer/components/Layout/TitleBar.workbench.menus.ts` and `src/renderer/hooks/appEventNames.ts` — menu wiring sources (Phase D).
12. `src/renderer/components/Layout/ChatOnlyShell/ChatWorkbenchArtifactPane.tsx`, `ArtifactHistoryList.tsx`, `useArtifactHistoryStack.ts` — artifact pane redesign sites (Phase G).
13. `src/renderer/hooks/useAgentEvents.ruleSkillReducers.ts` — composer-lag fix site (likely Phase F target).
14. `src/renderer/hooks/useContextPreview.ts` and `ComposerContextPreview.tsx` — context popover sites (Phase H + Phase F project-rules fix).
15. `roadmap/follow-ups/follow-ups.md` and `roadmap/audit-verification-pass.md` — Phase I source material.
16. `~/.claude/rules/manual-smoke-gate.md` — non-negotiable wave-end gate for UI-bearing changes; Phase J reads + complies.

## Note to the implementer

This wave is structurally unusual: it's a coordinated cleanup of 15 unrelated bugs surfaced in one user bug-hunt session. The temptation will be to treat each bug as a one-off and fix it in isolation. Resist that — Phase A's audit and Phase E's diagnosis are deliberately upfront so the implementer can see which bugs share root causes (the edit-mode triad in #9 is likely all one re-mount cascade; the workbench menu items are probably all the same dispatch-without-listener pattern). Fixing the root cause once is cheaper and lower-risk than fixing each symptom independently.

DO NOT take the opportunity to redesign the chat-only / workbench IA, restructure the title-bar menus, or rework the agent-events context model. The composer-lag fix in Phase F is a focused coalescing change, not a context-API refactor — if Phase E's diagnosis recommends a deeper change, surface that to orchestrator and file as a follow-up wave. DO NOT touch surfaces outside the bug list (TerminalManager internals, AgentMonitor, contextLayer, codebaseGraph) — every bug's fix scope was identified during the bug-hunt session; keep the fix to that scope.

Phases B (mechanical) and I (docs) are Haiku-friendly batches — don't escalate them to Sonnet "to be safe." Phases C, D, F, G, H need Sonnet judgment. Phase E is Sonnet-diagnostician specifically (root-cause investigation discipline matters here — don't propose fixes before evidence is collected, per `~/.claude/rules/debug-before-fix.md`). Phase A is Sonnet-architect.

Phase J's manual smoke is non-negotiable per `~/.claude/rules/manual-smoke-gate.md`. The chat-only / workbench shell is exactly the kind of UI-bearing surface where green tests don't catch the actual user experience — flashes, frozen menus, invisible bug states. Cole signs the checklist or the wave doesn't ship.

Before declaring a phase complete, restate the observation point from the Verification table in your own words and describe what you actually observed there. If you could not observe it directly — no live IDE, no triggered chat session, no rendered panel — say so explicitly. Do not substitute "tests pass" for runtime observation. Tests passing at the unit boundary is necessary but not sufficient.

## Orchestrator dispatch checklist

1. Verify ADR exists at `roadmap/wave-82-{slug}/wave-82-decisions.md`. Confirm decisions 1-8 are recorded in `Context / Pick / Rationale` form. **Pause for Cole's lock on decisions 9-12** before dispatching A. Phases B/C/D/E/F/I are unblocked by 9-12 and can begin during the wait if delay >24h.
2. Phase A — `sonnet-architect` — produce `phase-a-audit.md` with three deliverables (wiring matrix, dependency graph, chat-delete sequence diagram). Gate: diagram terminus is the user-observable endpoint stated in the Phases table Notes (chat-only sidebar visibly removing deleted row with no flash). Audit reviewed by orchestrator before dispatching dependent phases.
3. **Parallel batch dispatch:**
   - Phase B (4 parallel haiku-implementer workers via sonnet-batch-coordinator-produced plan): B1 label, B2 heat map, B3 status bar, B4 terminal close. Gate: each worker's scoped vitest passes; orchestrator merges and runs `npm run test:renderer`.
   - Phase C — `sonnet-implementer` — chat-delete + outer-rail persistence + activeproject desync. Gate: Cole verifies in live chat-only window: delete-no-flash, add-then-restart persistence, desync clearing.
   - Phase D — `sonnet-implementer` — workbench menu wiring sweep. Gate: Cole verifies every menu item in chat-only fires its action OR has been removed.
   - Phase E — `sonnet-diagnostician` — diagnostic sprint. Gate: `phase-e-diagnosis.md` contains evidence-backed root-cause finding for each of the 4 threads (#6, #9, #13-freeze, #15).
   - Phase I — `haiku-research-extractor` — follow-ups digest. Gate: `outstanding-2026-05-03.md` exists with categorized digest.
4. Phase F — `sonnet-implementer` — diagnostic-driven fixes. Blocked on Phase E. Gate: Cole verifies each fix in live IDE per the Phase F observation rows.
5. Phase G — `sonnet-implementer` — artifact pane redesign. Blocked on Phase 0 decision 9 lock; integrates Phase F edit-mode fix. Gate: Cole verifies 6-file open-and-overflow + Recent-click LRU swap in live workbench.
6. Phase H — `sonnet-implementer` — context popover work. Blocked on Phase 0 decisions 11 & 12 locks. Gate: Cole verifies Skills tab populated, unified Context tab grouping (if picked), non-image drop pinning.
7. Phase J — orchestrator — full quality gates + manual smoke. Run: `npx tsc --noEmit`, `npm run lint`, full `timeout 360 npx vitest run`, `/review 82` mechanical gap-check. Manual smoke checklist in `wave-82-result.md` — Cole signs after walking every observation row above. On full pass: commit + push + result brief published.
8. Final wrap: orchestrator diff review of the entire wave; release tag bump (target v2.13.0); push to GitHub.
