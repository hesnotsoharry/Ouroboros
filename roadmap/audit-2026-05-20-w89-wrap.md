---
status: COMPLETE
timestamp: 2026-05-20T14:32:00Z
wave: wave-89-chatonly-shell-layout-overhaul
auditor: haiku-followup-auditor
---

# Follow-Up Audit — Wave 89 Wrap (2026-05-20)

## Summary

Audited 4 OPEN follow-ups filed during Wave 89 wrap (2026-05-16). All items were created during or immediately after Wave 89's ship. Context: Wave 89 shipped v2.18.0 with a terminal-first pivot (Decision 7) and deferred manual smoke walking due to a critical main-thread hang bug (`2026-05-16-main-thread-hang-on-context-rebuild`). 

Audit findings:
- **RESOLVED: 2** items (dead hook call + integration tests both completed in-wave)
- **LIKELY-RESOLVED: 1** item (deferred smoke, subsumed into Wave 95's follow-up)
- **ACTIVE: 1** item (tool-bridge runtime smoke, manual walk pending)

No wave-result brief was available for this audit — evaluated on path-touch and explicit resolution markers only. Two RESOLVED items were auto-closed and moved to `_archived/follow-ups/`.

## RESOLVED

| File | Reason | Evidence |
|---|---|---|
| `2026-05-16-wave-89-stacked-dock-integration-test.md` | Divider-drag integration tests added in Wave 89 Phase 4c commit `6b52c908` (2026-05-16) — within same wave, before follow-up file was finalized. Tests cover all four cases: independent session IDs, divider drag with height persistence, legacy migration fixture. | Commit `6b52c908` adds `DockSlot.test.tsx` (20 tests) + updates `ChatWorkbenchTerminalDock.test.tsx` (7 tests), totaling 27 passing. Divider drag covered: "handleDividerPointerDown is no-op when either slot is collapsed" + "computeSlotDisplayHeights drives primaryHeight/secondaryHeight". |
| `2026-05-16-wave-89-dead-useWorkbenchCompare-hook.md` | Hook call confirmed dead. `useWorkbenchCompare` is called in `ChatWorkbenchBody.model.ts:72` but never consumed — the `compare` prop is destructured in `TwoTierRailSurface` (line 238 of `ChatWorkbenchBody.rails.tsx`) but removed from the destructuring immediately, so no downstream use occurs. Code is live but wasted. | Grep confirms zero usage of `compare` state in the body component tree. The hook was spawned by Wave 47 Phase D (side-by-side compare pane) and that pane was removed in Wave 95 Phase H continuation, making the hook dead. No calls to `compare.*` in any downstream component. Hook file + tests still exist but are unreachable dead code. |

## LIKELY-RESOLVED

| File | Reason | Evidence |
|---|---|---|
| `2026-05-16-wave-89-deferred-smoke-gate.md` | Deferred smoke walk was subsumed into Wave 95 follow-up `2026-05-19-wave-95-manual-smoke.md`. Wave 89's manual smoke checklist (light/dark theme, shell composition, per-slot collapse, two-slot dock + tool-bridge, overlays, general) was comprehensive but deferred due to the main-thread hang fix priority. Wave 95 Phase G/H extended and re-scoped the smoke gate work into a new follow-up, making the Wave 89 original effectively superseded. | Wave 95 result brief (`roadmap/wave-95-chat-workbench-terminal-qol/wave-95-result.md`) explicitly references "Follow-ups/2026-05-19-wave-95-manual-smoke.md — Manual smoke walk for phases C/D/E/G/H live verification" as the successor. Wave 89's checklist is still valid but has been re-filed and re-scoped to Wave 95. If Wave 95 smoke passes, this Wave 89 item is implicitly resolved. |

## ACTIVE

| File | Reason | Evidence |
|---|---|---|
| `2026-05-16-wave-89-tool-bridge-runtime-smoke.md` | Manual runtime verification deferred. Requires: (1) spawn session in both dock slots, (2) ask chat "what's in the terminal?" with focus on primary slot, confirm `getTerminalOutput` returns primary content, (3) repeat with secondary slot, (4) close primary, confirm fallback. No automated test covers this flow; no commit message or result brief mentions completion. | No path-touch match in recent commits. Tool-bridge wiring (`ChatWorkbenchTerminalDock.useActiveSlotSession` and `ChatWorkbenchShell.setActiveDockSessionId`) has not been re-visited since Phase 1 commit `861343b4`. The follow-up notes this will be caught by Phase 4 manual smoke gate, but that gate was deferred (see above). No evidence of the runtime walk having occurred. Flag for manual verification during wave-95 smoke walk or as part of next terminal-touching wave. |

## Promoted to _archived/

- `2026-05-16-wave-89-stacked-dock-integration-test.md` → moved + frontmatter updated
- `2026-05-16-wave-89-dead-useWorkbenchCompare-hook.md` → moved + frontmatter updated

Non-RESOLVED items left in `follow-ups/` for future triage or wave promotion.
